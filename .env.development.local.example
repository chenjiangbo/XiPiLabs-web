# 本地开发环境变量示例
# 复制为 .env.development.local 并根据需要修改。Next.js 在 dev 模式下会优先加载该文件，
# 因此不会影响生产使用的 .env.local / 其他配置。

NEXT_PUBLIC_BASE_URL=http://localhost:3210
PORT=3210
NODE_ENV=development
NODE_OPTIONS=--conditions=react-server

# --- 安全配置 ---
JWT_SECRET=dev-only-jwt-secret-change-me
OTP_DEV_MODE=true
OTP_CODE_LEN=6
OTP_TTL_SECONDS=300
OTP_MAX_ATTEMPTS=5
OTP_SEND_COOLDOWN_SECONDS=60

# --- 数据库 / Redis ---
# 将 host.docker.internal 替换成你的数据库/缓存地址
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/xipilabs_dev
REDIS_URL=redis://host.docker.internal:6379

# --- Google OAuth（本地测试可以创建一组测试凭证） ---
GOOGLE_CLIENT_ID=your-local-google-client-id
GOOGLE_CLIENT_SECRET=your-local-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3210/api/auth/callback/google

# --- 阿里云短信（如无需短信，可保持 OTP_DEV_MODE=true ） ---
ALIYUN_ACCESS_KEY_ID=your-aliyun-key-id
ALIYUN_ACCESS_KEY_SECRET=your-aliyun-key-secret
ALIYUN_SMS_SIGN_NAME=YourSign
ALIYUN_SMS_TEMPLATE_CODE=SMS_xxxxxx
ALIYUN_SMS_REGION=cn-hangzhou
